{
  "name": "native-protection-transformer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "(cd native-protection-transformer && npm i) && (cd pure-transformer && npm i) && (cd exemple && npm i)"
  },
  "author": "",
  "license": "ISC"
}
