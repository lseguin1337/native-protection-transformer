{
  "name": "native-protection-transformer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "(cd runtime-protection && npm i && npm run build) && (cd native-protection-transformer && npm i && npm run build) && (cd pure-transformer && npm i && npm run build) && (cd exemple && npm i && npm run build)"
  },
  "author": "",
  "license": "ISC"
}
